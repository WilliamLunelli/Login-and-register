{
  "name": "login-register-app",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd Backend && tsx src/index.ts",
    "frontend": "cd Frontend && live-server --port=3000 --cors"
  },
  "devDependencies": {
    "concurrently": "^7.6.0",
    "live-server": "^1.2.2"
  },
  "dependencies": {
    "tsx": "^4.20.5"
  }
}
